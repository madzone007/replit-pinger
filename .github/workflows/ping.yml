name: Keep Replit Alive

on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Replit
        run: curl -s "https://replit.com/@madzone70/bot20-mad" > /dev/null
