name: Keep Replit Alive

on:
  schedule:
    - cron: '*/1 * * * *'  # Runs every 1 minute, 24/7

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping Replit
      run: curl -s "https://my-247-bot.madzone70.repl.co" > /dev/null
